spring:
  application:
    name: User4
  datasource:
    url: jdbc:postgresql://localhost:5432/Jdbc
    username: postgres
    password: 9704
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
